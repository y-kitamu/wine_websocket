#!/bin/bash
# DISPLAY が未設定の場合、X11 ソケットを自動検出
if [ -z "$DISPLAY" ]; then
    for sock in /tmp/.X11-unix/X*; do
        [ -e "$sock" ] && export DISPLAY=":${sock##*X}" && break
    done
fi

WINEDLLOVERRIDES="websocket=native" wine ~/.wine/drive_c/users/${USER}/AppData/Local/kabuStation/KabuS.exe "$@"
